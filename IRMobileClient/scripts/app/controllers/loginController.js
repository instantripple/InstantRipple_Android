(function () {
    var irApp = angular.module('irApp');

    irApp.controller('loginController', [
        '$state', 'clientSession',
        function ($state, clientSession) {
        }
    ]);
})();